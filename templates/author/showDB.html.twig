<html>
    {{form_start(f)}}
    {{form_label(f.min)}}
    {{form_widget(f.min)}}
    {{form_label(f.max)}}
    {{form_widget(f.max)}}
    {{form_end(f)}}
    <body>
        
    
        <table border="4">
            <tr>
                <td>ID</td>
                <td>Username</td>
                <td>E-mail</td>
                <td>Number of books</td>
                <td>Actions</td>
            </tr>
            {%for a in auth%}
            <tr>
                <td>{{a.id}}</td>
                <td>{{a.username}}</td>
                <td>{{a.email}}</td>
                <td>{{a.getNbBooks}}</td>
                <td><a href="{{path('editAuthor',{'id':a.id})}}">Edit</a>
                <a href="{{path('deleteAuthor',{'id':a.id})}}">Delete</a></td>
            </tr>
            {%endfor%}
        </table>
        <a href="{{path('addAuthor')}}">Add here!</a>
        <a href="{{path('showdbzero')}}"><button>Delete authors with zero books</button></a>
    </body>
</html>